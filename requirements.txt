# ASR – faster-whisper (offline, no triton, Windows-compatible)
faster-whisper>=1.0.0

# TTS – Kokoro ONNX (offline, high quality)
kokoro-onnx>=0.4.6

# ONNX runtime with GPU support (includes CPU fallback)
onnxruntime-gpu>=1.20.0

# Audio I/O
sounddevice>=0.4.6
numpy<2

# Input
keyboard>=0.13.5

# UI
rich>=13.7.0

# Fallback TTS (Windows SAPI5, fully offline)
pyttsx3>=2.90